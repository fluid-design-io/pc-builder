import type { Lists } from ".keystone/types";
import { User, Role, Order, OrderItem, Address, OrderStatus } from "./";

export const lists: Lists = {
  User,
  Role,
  Order,
  OrderItem,
  OrderStatus,
  Address,
};
